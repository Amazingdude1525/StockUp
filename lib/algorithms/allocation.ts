type Candidate = { id:string; code:string; loadPercent:number; items:Array<{productId:string;requested:number;available:number;bins:number;approxDistance:number}> };
export function selectWarehouse(candidates:Candidate[]) { return candidates.map(w=>{const covered=w.items.filter(i=>i.available>=i.requested).length,total=w.items.length,full=covered===total,unitRatio=w.items.reduce((s,i)=>s+Math.min(i.available,i.requested),0)/Math.max(1,w.items.reduce((s,i)=>s+i.requested,0)),distance=w.items.reduce((s,i)=>s+i.approxDistance,0),risk=w.items.filter(i=>i.available-i.requested<5).length/Math.max(1,total),score=(full?1000:0)+(covered/Math.max(1,total))*300+unitRatio*200-(w.loadPercent/100)*150-Math.min(1,distance/600)*100-risk*120;return{...w,full,covered,total,unitRatio,distance,risk,score}}).sort((a,b)=>b.score-a.score) }
